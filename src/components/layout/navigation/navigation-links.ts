// Re-export from unified navigation config for backwards compatibility
export { mainNavigation as navigationLinks, authenticatedNavigation } from '@/config/navigation';
export type { NavigationItem as NavigationLink } from '@/config/navigation';
